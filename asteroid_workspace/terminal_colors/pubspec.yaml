name: terminal_colors
description: ANSI terminal color formatting library.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '^3.8.1'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0